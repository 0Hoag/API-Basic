spring:
  application:
    name: identity-service
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/dethi}
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: 601748
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  servlet:
    context-path: /api/v1/identity

jwt:
  signerKey: "RREniFvR4thHKZvOOSbmIgCCi59YC/qcJwNiAbl/50w1lldEjpnBXXpd5LGkYofz"
  valid-duration: 6000 # in second
  refreshtable-duration: 1200 # in seconds
